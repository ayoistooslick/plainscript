bring persistEvent from "./events.pln"

web app
require api key from env("WEBHOOK_KEY")

route post "/webhooks/events"
    remember event as body()
    persistEvent(event)
    show "Webhook accepted"
    status 202
    reply json
        accepted is true
    done
done

start 3000