bring issueToken from "./auth.pln"

web app
enable sessions env("SESSION_SECRET")
require api key from env("API_KEY")

route get "/api/me"
    reply json
        ok is true
        user is "authenticated"
    done
done

route post "/api/token"
    remember token as issueToken(body("user"), env("SESSION_SECRET"))
    reply json
        token is token
    done
done

start 3000