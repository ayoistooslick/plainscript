# PostgreSQL app

A welcoming PostgreSQL REST API starter with health checks, a users table, and
JSON routes for reading and inserting rows. Point it at any Postgres connection
string and it's ready to go.

```bash
npx plainscript build src/app.pln -o dist/app.js && node dist/app.js
```
