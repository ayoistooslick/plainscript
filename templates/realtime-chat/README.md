# Realtime chat

A minimal WebSocket chat starter that broadcasts messages to everyone connected
and stores a history you can read and append over REST. Fun and ready to extend.

```bash
npx plainscript build src/app.pln -o dist/app.js && node dist/app.js
```
