// PlainScript 1.0.36: fetch an upstream API from a route.
web app
route get "/proxy"
    remember upstream as get "https://example.com/data" timeout 5000
    reply json
        status is upstream.status
        data is upstream.body
    done
done
start 3000