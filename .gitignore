# Secrets — never commit
.env
.env.*
!.env.example

# Node
node_modules/

# Plain AI translation cache (RFC-0020 §15, §44)
.plain/ai-cache/
ai-cache/

# Build output
_plain_out.js
*.js.map
dist/

# Logs
*.log
log.txt

# Scratch / ad-hoc test artifacts
data
out.bin
output.txt

# Agent memory and local dev config
.agents/memory/MEMORY.md
.replit
.claude/
.aider*

# Local roadmap / task tracker (never commit)
TASKS.md
tasks.md

# AI Datasets (never commit large generated corpus files)
dataset/
*.jsonl

# Extension and package binaries
*.vsix
*.tgz
release-artifacts/

# Rust and native build artifacts
target/
crates/**/target/

# Package registry cache
.pln_modules/
.plainscript/vendor/

# Local Drip Feed Commit System (never commit)
scripts/
tools/drip-commit.*
.drip-feed/
drip-feed.json
drip-commit.json
